experiment in interpreters<br>
lexical scoping, full ast-walk<br>
currently supports:<br>
strings, ints, arrays can all be printed with print();<br>

TODO<BR>
implement short-circuit logic -> break and continue -> for loops
