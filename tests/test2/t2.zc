fun outer(a) {
	
	let counter = 0;

	fun inner(b) {
		print(counter);
		counter = counter + 1;
	}

	while (counter < a) {
		inner(counter);
	}

	return counter;

}

fun main() {
	let a = outer(10);
	print(a);
}

main();
