fun fib(value) {
	if (value == 0) {
		return 0;
	} else if (value == 1) {
		return 1;
	}
	return fib(value - 1) + fib(value - 2);
}

fun main() {
	let a = 10;
	let counter = 1;
	while (counter <= a) {
		print(fib(counter));
		counter = counter + 1;
	}
}

main();
